[project]
name = "edgetx-sdcard-sounds"
version = "0.1.0"
description = "EdgeTX SD card sounds generator"
requires-python = ">=3.10"
dependencies = [
    "azure-cognitiveservices-speech==1.44.0",
    "colorama",
    "ffmpeg-normalize",
    "rich",
    "elevenlabs",
    "python-dotenv",
]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
# Allow lines to be as long as 120.
line-length = 120
